# Extra packages for claude_app.py (the PySide6 desktop GUI), on top of
# the upstream requirements.txt (pyserial, Pillow, etc.) already needed by claude_screen.
PySide6~=6.11.1   # Qt6 GUI
tzdata~=2026.2    # zoneinfo data on Windows so the weekly anchor survives DST shifts
