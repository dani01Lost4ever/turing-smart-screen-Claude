# Core requirements for the Claude Code Status Buddy widget (claude_screen.py)
# and the reused library/lcd panel driver. Pinned for Python 3.13.
pyserial~=3.5      # serial link to the Turing USB panel
Pillow~=12.2.0     # dashboard rendering
numpy~=2.4.4       # fast image serialization for the panel

# The desktop app (claude_app.py) needs extra packages: see requirements-app.txt.
# Optional: `pip install claude-monitor` adds a P90 fallback for the 5h gauge
# when the OAuth usage endpoint is unavailable (it's normally exact without it).
